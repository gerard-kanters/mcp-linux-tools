# MCP Linux Tools Server dependencies
# Install with: pip install -r requirements.txt --break-system-packages

# Required
fastmcp>=2.14.0

# Optional (used for cron schedule validation)
croniter>=1.0.0

